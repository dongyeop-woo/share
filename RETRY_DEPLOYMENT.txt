새로운 압축 파일 전송 완료!

SSH 터미널에서 다음 명령어를 다시 실행하세요:

========================================
cd ~
sudo cp ~/share-deploy.tar.gz /tmp/share-deploy.tar.gz
sudo ./cleanup-and-deploy.sh
========================================

이번에는 정상적으로 작동할 것입니다.

